import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "GHOST BOARD | Autonomous Executive Coordination System",
  description: "Futuristic enterprise AI command center for autonomous executive coordination.",
};

import { OrchestrationProvider } from "@/context/orchestration-context";

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
      suppressHydrationWarning
    >
      <body className="min-h-full flex flex-col">
        <OrchestrationProvider>
          {children}
        </OrchestrationProvider>
      </body>
    </html>
  );
}
